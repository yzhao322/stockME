{{!-- <nav class="navbar navbar-default" >
    <div class="container-fluid">
        <div class="navbar-header" >
           
        </div>
    </div>
</nav> --}}
<nav class="navbar navbar-default" style="text-align: center">
    <div class="container">
        <div class="row">
            <div class="col-sm-3" style="float:left">
                <h4>Master page</h4>
            </div>
            <div class="col-sm-6">
                <h1 class="stockMe-logo">StockME</h1>
            </div>
            <div class="col-sm-3" style="float:right"></div>

            <a href="/logout"><h4>- Logout -</h4></a>


        </div>
    </div>
</nav>

<div class="row" style="text-align: center;border: 5px dashed #bababa;">
    <h1>Master page</h1>

    <div class="col-sm-6">
        <h3 class="master-list">Member List</h3>
        <ul>
            {{#each usermember}}
            <p id="member">{{this.email}} - {{this.title}}
                <form class="upgrade" name="{{this.email}}">
                    <button type="submit" class="btn btn-default submit" id="upgrade">Upgrade to Manager!
                    </button> </form>
            </p>
            {{/each}}

        </ul>
    </div>

    <div class="col-sm-6" style="text-align: center;">
        <h3 class="master-list">Manager List</h3>
        <ul>
            {{#each usermanager}}
            <p id="manager">{{this.email}} - {{this.title}}
                <form class="degrade" name="{{this.email}}">
                    <button type="submit" class="btn btn-default submit" id="degrade">Degrade to Member!
                    </button> </form>
            </p>
            {{/each}}

        </ul>
    </div>


</div>